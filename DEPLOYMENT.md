# Déploiement

La procédure de référence se trouve dans `docs/deployment-vercel.md` et `docs/production.md`.

Avant mise en ligne : compléter les données légales et locales, configurer Supabase, Stripe, Resend, le domaine, `CRON_SECRET` et les flux iCal, puis suivre `docs/checklist.md`.
