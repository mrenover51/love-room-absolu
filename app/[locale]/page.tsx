import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { notFound } from "next/navigation";
import { Check, Search } from "lucide-react";
import { Footer } from "@/components/layout/footer";
import { Header } from "@/components/layout/header";
import { JsonLd } from "@/components/seo/json-ld";
import { CurrencySelector } from "@/components/i18n/currency-selector";
import { dictionaries, isLocale, languageAlternates, locales } from "@/lib/i18n/config";
import { siteConfig } from "@/lib/site-config";
export const dynamicParams=false;
export function generateStaticParams(){return locales.map(locale=>({locale}));}
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;if(!isLocale(locale))return{};const d=dictionaries[locale],url=`${siteConfig.url}/${locale}`;return {title:d.title,description:d.description,alternates:{canonical:url,languages:{...languageAlternates(),"x-default":siteConfig.url}},openGraph:{type:"website",locale,alternateLocale:locales.filter(x=>x!==locale),url,title:d.title,description:d.description,images:[{url:`${siteConfig.url}/opengraph-image`,width:1200,height:630,alt:d.hero}]},twitter:{card:"summary_large_image",title:d.title,description:d.description,images:[`${siteConfig.url}/opengraph-image`]}};}
export default async function InternationalHome({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const d=dictionaries[locale],url=`${siteConfig.url}/${locale}`,schema={"@context":"https://schema.org","@graph":[{"@type":"WebPage","@id":url,url,name:d.title,description:d.description,inLanguage:locale,isPartOf:{"@id":`${siteConfig.url}/#website`}},{"@type":"FAQPage",inLanguage:locale,mainEntity:d.faq.map(item=>({"@type":"Question",name:item.q,acceptedAnswer:{"@type":"Answer",text:item.a}}))}]};return <><Header locale={locale}/><main lang={locale}><JsonLd data={schema}/><section className="relative flex min-h-[88svh] items-end overflow-hidden pb-20 pt-32"><Image src="/images/optimized/lit.avif" alt={d.hero} fill priority sizes="100vw" className="object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-black via-black/45 to-black/20"/><div className="page-shell relative"><p className="eyebrow text-[#C9A86A]">{d.eyebrow}</p><h1 className="mt-5 max-w-5xl font-heading text-6xl sm:text-8xl">{d.hero}</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-white/75">{d.intro}</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/reservation" className="rounded-full bg-[#C9A86A] px-6 py-3 font-semibold text-black">{d.book}</Link><Link href="/la-suite" className="rounded-full border border-white/30 px-6 py-3">{d.discover}</Link></div></div></section><section id="suite" className="page-shell grid gap-5 py-20 md:grid-cols-3">{d.facts.map((fact, keyIndex)=><div key={`${fact}-${keyIndex}`} className="rounded-2xl border border-white/10 p-6"><Check className="size-5 text-[#C9A86A]"/><p className="mt-4">{fact}</p></div>)}</section><section className="bg-[#F2ECE4] py-20 text-[#201B18]"><div className="page-shell grid gap-12 lg:grid-cols-2"><div id="magazine"><h2 className="font-heading text-5xl">{d.blogTitle}</h2><ol className="mt-7 space-y-4">{d.posts.map((title,index)=><li key={`${title}-${index}`} className="rounded-xl border border-black/10 bg-white p-5"><span className="mr-3 text-[#8B6B36]">0{index+1}</span>{title}</li>)}</ol></div><div id="guide"><h2 className="font-heading text-5xl">{d.guideTitle}</h2><ul className="mt-7 space-y-4">{d.guides.map((title, keyIndex)=><li key={`${title}-${keyIndex}`} className="rounded-xl border border-black/10 bg-white p-5">{title}</li>)}</ul></div></div></section><section id="faq" className="page-shell py-20"><h2 className="font-heading text-5xl">{d.faqTitle}</h2><div className="mt-8 divide-y divide-white/10">{d.faq.map((item, keyIndex)=><details key={`${item.q}-${keyIndex}`}><summary className="cursor-pointer py-5 font-heading text-2xl">{item.q}</summary><p className="max-w-3xl pb-6 leading-8 text-white/55">{item.a}</p></details>)}</div><form action={`/${locale}/recherche`} className="mt-14 flex max-w-xl gap-2"><label className="sr-only" htmlFor="international-search">{d.search}</label><input id="international-search" name="q" placeholder={d.search} className="min-h-12 flex-1 rounded-full border border-white/15 bg-black px-5"/><button className="grid size-12 place-items-center rounded-full bg-[#C9A86A] text-black" aria-label={d.search}><Search className="size-5"/></button></form><div className="mt-8 max-w-xl"><CurrencySelector locale={locale}/></div></section></main><Footer/></>}
