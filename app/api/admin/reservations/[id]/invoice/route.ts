import { isAdminRequest } from "@/lib/admin/api-auth";
import { createAdminClient } from "@/lib/supabase/admin";

const escape=(value:unknown)=>String(value??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
export async function GET(_request:Request,{params}:{params:Promise<{id:string}>}){if(!await isAdminRequest())return new Response("Non autorisé",{status:401});const{id}=await params,{data}=await createAdminClient().from("reservations").select("reference,created_at,guest_first_name,guest_last_name,guest_email,check_in,check_out,nights,subtotal,extras_total,taxes,total,currency,payment_status,stripe_payment_intent,reservation_options(label,quantity,price,total)").eq("id",id).maybeSingle();if(!data)return new Response("Facture introuvable",{status:404});const options=(data.reservation_options??[]).map(item=>`<tr><td>${escape(item.label)} × ${item.quantity}</td><td>${(item.total/100).toFixed(2)} €</td></tr>`).join(""),html=`<!doctype html><html lang="fr"><head><meta charset="utf-8"><title>Facture ${escape(data.reference)}</title><style>body{font-family:Arial,sans-serif;color:#1d1a17;max-width:760px;margin:40px auto;padding:24px}h1{font-family:Georgia,serif}header{display:flex;justify-content:space-between;border-bottom:1px solid #ddd;padding-bottom:24px}table{width:100%;border-collapse:collapse;margin-top:32px}td{padding:12px;border-bottom:1px solid #eee}td:last-child{text-align:right}.total{font-size:22px;font-weight:700}.muted{color:#777;font-size:13px}</style></head><body><header><div><h1>ABSOLU</h1><p>Suite romantique · Avize</p></div><div><strong>Facture ${escape(data.reference)}</strong><p class="muted">Émise le ${new Date(data.created_at).toLocaleDateString("fr-FR")}</p></div></header><h2>${escape(data.guest_first_name)} ${escape(data.guest_last_name)}</h2><p>${escape(data.guest_email)}</p><p>Séjour du ${escape(data.check_in)} au ${escape(data.check_out)} · ${data.nights} nuit(s)</p><table><tr><td>Hébergement</td><td>${(data.subtotal/100).toFixed(2)} €</td></tr>${options}<tr><td>Taxes</td><td>${(data.taxes/100).toFixed(2)} €</td></tr><tr class="total"><td>Total réglé</td><td>${(data.total/100).toFixed(2)} €</td></tr></table><p class="muted">Statut : ${escape(data.payment_status)} · Paiement Stripe : ${escape(data.stripe_payment_intent)}</p></body></html>`;return new Response(html,{headers:{"content-type":"text/html; charset=utf-8","content-disposition":`attachment; filename="facture-${data.reference}.html"`,"cache-control":"private, no-store"}})}
