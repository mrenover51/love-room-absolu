import { NextResponse } from "next/server";
export function GET(request: Request) { const url = new URL(request.url), reference = url.searchParams.get("reference") ?? "Réservation", checkIn = url.searchParams.get("checkIn") ?? "", checkOut = url.searchParams.get("checkOut") ?? ""; if (!/^\d{4}-\d{2}-\d{2}$/.test(checkIn) || !/^\d{4}-\d{2}-\d{2}$/.test(checkOut)) return NextResponse.json({ error: "Dates invalides" }, { status: 400 }); const stamp = new Date().toISOString().replace(/[-:]/g, "").replace(/\.\d{3}Z$/, "Z"), start = `${checkIn.replaceAll("-", "")}T150000Z`, end = `${checkOut.replaceAll("-", "")}T100000Z`, ics = ["BEGIN:VCALENDAR", "VERSION:2.0", "PRODID:-//Absolu//Reservation//FR", "BEGIN:VEVENT", `UID:${encodeURIComponent(reference)}@love-room-absolu.fr`, `DTSTAMP:${stamp}`, `DTSTART:${start}`, `DTEND:${end}`, "SUMMARY:Parenthèse Absolu", "LOCATION:36 rue Pasteur\, 51190 Avize", `DESCRIPTION:Réservation ${reference}`, "END:VEVENT", "END:VCALENDAR"].join("\r\n"); return new Response(ics, { headers: { "Content-Type": "text/calendar; charset=utf-8", "Content-Disposition": "attachment; filename=reservation-absolu.ics", "Cache-Control": "no-store" } }); }
