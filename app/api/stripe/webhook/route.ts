import type Stripe from "stripe";import {stripeProvider} from "@/lib/stripe/stripe-provider";import {PaymentService} from "@/lib/stripe/payment-service";import {createAdminClient} from "@/lib/supabase/admin";import {sendConfirmation,sendStatusEmail} from "@/lib/email";
export const runtime="nodejs";type EmailRow={reference:string;guest_first_name:string;guest_email:string;check_in:string;check_out:string;total:number};const emailData=(row:EmailRow)=>({reference:row.reference,firstName:row.guest_first_name,email:row.guest_email,checkIn:row.check_in,checkOut:row.check_out,totalAmount:row.total});
export async function POST(request:Request){const signature=request.headers.get("stripe-signature");if(!signature)return new Response("Signature absente",{status:400});let event:Stripe.Event;try{event=stripeProvider.constructWebhook(await request.text(),signature)}catch{return new Response("Signature invalide",{status:400})}const payments=new PaymentService();try{if(!await payments.claimEvent(event.id,event.type))return new Response("OK");const db=createAdminClient();if(event.type==="checkout.session.completed"){const session=event.data.object,id=session.metadata?.reservation_id;if(!id||session.payment_status!=="paid")throw new Error("INVALID_SESSION");const changed=await payments.confirm(id,session.id,typeof session.payment_intent==="string"?session.payment_intent:null);if(changed){const{data}=await db.from("reservations").select("reference,guest_first_name,guest_email,check_in,check_out,total").eq("id",id).single();if(data)await sendConfirmation(emailData(data))}}else if(event.type==="checkout.session.expired"){const id=event.data.object.metadata?.reservation_id;if(id){const{data}=await db.from("reservations").update({status:"cancelled",payment_status:"failed",updated_at:new Date().toISOString()}).eq("id",id).eq("status","pending_payment").select("reference,guest_first_name,guest_email,check_in,check_out,total").maybeSingle();if(data)await sendStatusEmail("failed",emailData(data))}}else if(event.type==="charge.refunded"){const charge=event.data.object,pi=typeof charge.payment_intent==="string"?charge.payment_intent:charge.payment_intent?.id;if(pi){const full=charge.amount_refunded>=charge.amount,{data}=await db.from("reservations").update({status:full?"refunded":"confirmed",payment_status:full?"refunded":"partially_refunded",updated_at:new Date().toISOString()}).eq("stripe_payment_intent",pi).select("reference,guest_first_name,guest_email,check_in,check_out,total").maybeSingle();if(full&&data)await sendStatusEmail("refunded",emailData(data))}}return new Response("OK")}catch(error){await payments.releaseEvent(event.id);console.error("stripe_webhook_failed",{eventId:event.id,type:event.type,code:error instanceof Error?error.message:"UNKNOWN"});return new Response("Erreur temporaire",{status:500})}}
