import {Footer} from "@/components/layout/footer";import {Header} from "@/components/layout/header";import {MagazineExplorer} from "@/components/magazine/magazine-explorer";import {magazineArticles,magazineCategories,magazineTags} from "@/lib/magazine/articles";import {pageMetadata} from "@/lib/seo";
export const metadata=pageMetadata({title:"Rechercher dans le Magazine Absolu",description:"Recherchez parmi plus de 100 articles sur la Champagne, les escapades romantiques, la gastronomie et le bien-être à deux.",path:"/blog/recherche",index:false});const cards=magazineArticles.map(({slug,title,excerpt,category,tags,image,readingTime})=>({slug,title,excerpt,category,tags,image,readingTime}));export default function SearchPage(){return <><Header/><main className="page-shell pb-28 pt-36"><p className="eyebrow text-[#C9A86A]">Recherche</p><h1 className="mt-5 font-heading text-6xl sm:text-8xl">Explorer le magazine</h1><p className="mt-6 max-w-2xl leading-8 text-white/55">Filtrez les articles par mot-clé, catégorie ou tag.</p><div className="mt-12"><MagazineExplorer articles={cards} categories={[...magazineCategories]} tags={magazineTags}/></div></main><Footer/></>}
