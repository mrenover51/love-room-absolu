import type {Metadata} from 'next';import Link from 'next/link';import {Header} from '@/components/layout/header';import {Footer} from '@/components/layout/footer';export const metadata:Metadata={title:'Recherche | Absolu',robots:{index:false,follow:true}};const pages=[['La Suite','/la-suite'],['Galerie','/galerie'],['Équipements','/equipements'],['Réservation','/reservation'],['Contact','/contact'],['FAQ','/faq']];export default async function Search({searchParams}:{searchParams:Promise<{q?:string}>}){const q=(await searchParams).q?.trim().toLocaleLowerCase('fr')??'',results=q?pages.filter(([label])=>label.toLocaleLowerCase('fr').includes(q)):pages;return <><Header/><main className="min-h-[80svh] bg-[#090909] px-5 pb-24 pt-36"><div className="mx-auto max-w-3xl"><h1 className="font-heading text-5xl">Recherche</h1><form className="mt-8 flex gap-3"><label htmlFor="q" className="sr-only">Votre recherche</label><input id="q" name="q" defaultValue={q} className="min-h-12 flex-1 border border-white/15 bg-[#121212] px-4"/><button className="bg-[#C9A86A] px-5 text-black">Rechercher</button></form><ul className="mt-8 space-y-3">{results.map(([label,href])=><li key={href}><Link href={href} className="block border-b border-white/10 py-4 text-xl hover:text-[#C9A86A]">{label}</Link></li>)}</ul></div></main><Footer/></>}
