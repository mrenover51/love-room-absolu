import type { Metadata } from "next";import { Header } from "@/components/layout/header";import { Footer } from "@/components/layout/footer";import { ConfirmationDetails } from "@/components/reservation/confirmation-details";import { bookingRepository } from "@/lib/booking/repository";
export const metadata:Metadata={title:"Demande reçue | Absolu",description:"Confirmation de votre demande de réservation auprès de la Suite Absolu.",robots:{index:false,follow:false}};
export default async function ConfirmationPage({searchParams}:{searchParams:Promise<{reference?:string}>}){const {reference}=await searchParams;const booking=reference?(await bookingRepository.list()).find(item=>item.reference===reference):undefined;const summary=booking?{reference:booking.reference,checkIn:booking.checkIn,checkOut:booking.checkOut,guestCount:booking.guestCount,pricing:booking.pricing}:undefined;return <><Header/><main className="min-h-[85svh] bg-[#080808] px-4 pb-20 pt-36"><ConfirmationDetails reference={reference} summary={summary}/></main><Footer/></>}
