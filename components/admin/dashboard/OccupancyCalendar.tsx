import Link from "next/link";import type {CalendarDay} from "@/lib/dashboard/dashboard-data";import {Card,SectionTitle} from "./dashboard-ui";const colors={available:"bg-emerald-400",booked:"bg-rose-500",arrival:"bg-orange-400",departure:"bg-blue-400",blocked:"bg-zinc-500"};
export function OccupancyCalendar({days}:{days:CalendarDay[]}){return <Card className="p-5 sm:p-7"><SectionTitle eyebrow="Disponibilités" title="Les 30 prochains jours"/><div className="mt-6 grid grid-cols-5 gap-2 sm:grid-cols-10">{days.map(d=>{const content=<><span className="text-[9px] uppercase text-white/35">{d.weekday}</span><strong className="mt-1 text-base">{d.day}</strong><span className={`mt-2 size-1.5 rounded-full ${colors[d.state]}`}/></>;return d.reservationId?<Link title={d.state} key={d.date} href={`/admin/reservations/${d.reservationId}`} className="flex min-h-20 flex-col items-center justify-center rounded-xl border border-white/[.06] bg-white/[.025] transition hover:border-[#C8A66A]/40 hover:bg-white/[.06]">{content}</Link>:<div title={d.state} key={d.date} className="flex min-h-20 flex-col items-center justify-center rounded-xl border border-white/[.06] bg-white/[.025]">{content}</div>})}</div><div className="mt-5 flex flex-wrap gap-x-4 gap-y-2 text-[10px] text-[#B8B2A8]">{Object.entries({available:"Libre",booked:"Réservé",arrival:"Arrivée",departure:"Départ",blocked:"Bloqué"}).map(([key,label])=><span key={key}><i className={`mr-1.5 inline-block size-2 rounded-full ${colors[key as keyof typeof colors]}`}/>{label}</span>)}</div></Card>}
