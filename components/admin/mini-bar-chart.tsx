export function MiniBarChart({data,label}:{data:number[];label:string}){const max=Math.max(...data,1);return <figure className="rounded-2xl border border-white/10 bg-[#121212] p-6"><figcaption className="mb-6 font-heading text-2xl">{label}</figcaption><div className="flex h-40 items-end gap-2" role="img" aria-label={`${label}, graphique sur ${data.length} périodes`}>{data.map((value,index)=><div key={index} className="group relative flex-1 rounded-t bg-gradient-to-t from-[#8E48FF]/40 to-[#C9A86A]" style={{height:`${Math.max(4,value/max*100)}%`}}><span className="sr-only">Période {index+1} : {value}</span></div>)}</div></figure>}
