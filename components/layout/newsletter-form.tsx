"use client";
import { useState,type FormEvent } from "react";
export function NewsletterForm(){const[state,setState]=useState<"idle"|"pending"|"sent"|"error">("idle");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget,email=String(new FormData(form).get("email")??"");if(!/^\S+@\S+\.\S+$/.test(email)){setState("error");return}setState("pending");try{const response=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name:"Inscription newsletter",email,message:"Demande d’inscription à la newsletter Absolu.",website:""})});if(!response.ok)throw new Error();setState("sent");form.reset()}catch{setState("error")}}return <form onSubmit={submit} className="mt-5" noValidate><label htmlFor="newsletter-email" className="sr-only">Votre adresse email</label><div className="flex border-b border-white/20"><input id="newsletter-email" name="email" type="email" autoComplete="email" placeholder="Votre adresse email" className="min-w-0 flex-1 bg-transparent py-3 text-sm text-white placeholder:text-white/35"/><button disabled={state==="pending"} className="px-3 text-xs uppercase tracking-[.14em] text-[#C9A86A] disabled:opacity-50">S’inscrire</button></div><p role="status" className="mt-3 min-h-5 text-xs text-white/45">{state==="sent"?"Demande d’inscription envoyée.":state==="error"?"Vérifiez votre email ou réessayez plus tard.":"Recevez ponctuellement les nouvelles d’Absolu."}</p></form>}
