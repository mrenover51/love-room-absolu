import Image from "next/image";
import Link from "next/link";
import {Clock3} from "lucide-react";
export type ArticleCardData={slug:string;title:string;excerpt:string;category:string;tags:readonly string[];image:string;readingTime:number};
export function ArticleCard({article,priority=false}:{article:ArticleCardData;priority?:boolean}){return <article className="group overflow-hidden rounded-[1.6rem] border border-white/10 bg-[#121212]"><Link href={`/blog/${article.slug}`}><div className="relative aspect-[4/3] overflow-hidden"><Image src={article.image} alt={article.title} fill preload={priority} sizes="(max-width: 768px) 100vw, (max-width: 1200px) 50vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105"/></div><div className="p-6"><div className="flex items-center justify-between text-[10px] uppercase tracking-wider text-[#C9A86A]"><span>{article.category}</span><span className="flex items-center gap-1 text-white/40"><Clock3 className="size-3"/>{article.readingTime} min</span></div><h2 className="mt-4 font-heading text-3xl leading-tight group-hover:text-[#DCC18E]">{article.title}</h2><p className="mt-4 line-clamp-3 text-sm leading-7 text-white/50">{article.excerpt}</p><div className="mt-5 flex flex-wrap gap-2">{article.tags.slice(0,2).map(tag=><span key={tag} className="rounded-full border border-white/10 px-2.5 py-1 text-[10px] text-white/40">#{tag}</span>)}</div></div></Link></article>}
