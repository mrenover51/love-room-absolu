"use client";
import {useMemo,useState} from "react";
import {Search} from "lucide-react";
import {ArticleCard,type ArticleCardData} from "./article-card";
export function MagazineExplorer({articles,categories,tags}:{articles:ArticleCardData[];categories:string[];tags:string[]}){const[q,setQ]=useState("");const[category,setCategory]=useState("");const[tag,setTag]=useState("");const filtered=useMemo(()=>{const needle=q.trim().toLocaleLowerCase("fr");return articles.filter(article=>(!category||article.category===category)&&(!tag||article.tags.includes(tag))&&(!needle||`${article.title} ${article.excerpt} ${article.tags.join(" ")}`.toLocaleLowerCase("fr").includes(needle)))},[articles,category,q,tag]);return <div><div className="grid gap-3 rounded-2xl border border-white/10 bg-white/[.04] p-4 md:grid-cols-[1fr_auto_auto]"><label className="flex min-h-12 items-center gap-3 border border-white/10 px-4"><Search className="size-4 text-white/40"/><span className="sr-only">Rechercher un article</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Rechercher dans le magazine" className="w-full bg-transparent outline-none placeholder:text-white/30"/></label><select aria-label="Filtrer par catégorie" value={category} onChange={e=>setCategory(e.target.value)} className="min-h-12 border border-white/10 bg-[#121212] px-4"><option value="">Toutes les catégories</option>{categories.map((item, keyIndex)=><option key={`${item}-${keyIndex}`}>{item}</option>)}</select><select aria-label="Filtrer par tag" value={tag} onChange={e=>setTag(e.target.value)} className="min-h-12 border border-white/10 bg-[#121212] px-4"><option value="">Tous les tags</option>{tags.map((item, keyIndex)=><option key={`${item}-${keyIndex}`}>{item}</option>)}</select></div><p aria-live="polite" className="mt-5 text-sm text-white/40">{filtered.length} article{filtered.length>1?"s":""}</p><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(article=><ArticleCard key={article.slug} article={article}/>)}</div></div>}
