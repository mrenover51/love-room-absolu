import type { PublicPricingConfig } from "@/lib/booking/types";
import { formatAmount } from "@/lib/booking/pricing";
export function ExtrasSelector({selected,onChange,extras}:{selected:string[];onChange:(keys:string[])=>void;extras:PublicPricingConfig["extras"]}) { return <fieldset><legend className="font-heading text-3xl">Composez votre séjour</legend><p className="mt-2 text-sm text-white/55">Tarifs de démonstration, recalculés côté serveur lors de la demande.</p><div className="mt-7 grid gap-3 sm:grid-cols-2">{extras.filter((extra)=>extra.enabled!==false).map((extra)=><label key={extra.key} className={`cursor-pointer border p-5 transition-colors ${selected.includes(extra.key)?"border-[#C9A86A] bg-[#C9A86A]/10":"border-white/10 bg-[#121212]"}`}><input type="checkbox" className="mr-3 size-5 accent-[#C9A86A]" checked={selected.includes(extra.key)} onChange={(event)=>onChange(event.target.checked?[...selected,extra.key]:selected.filter((key)=>key!==extra.key))}/><strong>{extra.label}</strong>{extra.description&&<span className="mt-2 block pl-8 text-xs leading-5 text-white/50">{extra.description}</span>}<span className="mt-2 block pl-8 text-sm text-[#C9A86A]">{formatAmount(extra.amount)}</span></label>)}</div></fieldset> }
