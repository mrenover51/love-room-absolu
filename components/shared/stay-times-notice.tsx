import { Clock3 } from "lucide-react";
import { DEFAULT_STAY_SETTINGS, getStayCopy, type StaySettings } from "@/lib/stay-config";

export function StayTimesNotice({className="",settings=DEFAULT_STAY_SETTINGS}:{className?:string;settings?:StaySettings}){const copy=getStayCopy(settings);return <aside className={`rounded-[1.5rem] border border-[#C9A86A]/20 bg-[#C9A86A]/[.06] p-5 backdrop-blur-sm ${className}`} aria-label="Horaires d’arrivée et de départ"><div className="flex items-center gap-3"><Clock3 className="size-5 text-[#C9A86A]" aria-hidden="true"/><h2 className="font-heading text-2xl">Arrivée et départ</h2></div><div className="mt-4 space-y-2 text-sm leading-7 text-white/65"><p>{copy.arrivalText}</p><p>{copy.departureText}</p><div className="flex flex-wrap gap-2 pt-2">{settings.earlyCheckInEnabled&&<span className="rounded-full border border-[#C9A86A]/25 px-3 py-1 text-xs text-[#E4C68E]">Arrivée anticipée disponible{settings.earlyCheckInFee>0?` · ${settings.earlyCheckInFee.toLocaleString("fr-FR")} €`:""}</span>}{settings.lateCheckOutEnabled&&<span className="rounded-full border border-[#C9A86A]/25 px-3 py-1 text-xs text-[#E4C68E]">Départ tardif disponible{settings.lateCheckOutFee>0?` · ${settings.lateCheckOutFee.toLocaleString("fr-FR")} €`:""}</span>}</div><p className="pt-2 text-white/45">{copy.flexibilityText}</p></div></aside>}
