import {Text} from "@react-email/components";import type {BookingEmailData} from "@/lib/email";import type {StaySettings} from "@/lib/stay-config";import {EmailShell,emailText} from "./email-shell";
export function ReservationAdminEmail({reference,checkIn,checkOut,totalAmount,staySettings}:BookingEmailData&{staySettings:StaySettings}){return <EmailShell preview={`Nouvelle réservation ${reference}`} title="Nouvelle réservation"><Text style={emailText}>Référence : <strong>{reference}</strong></Text><Text style={emailText}>Séjour du {checkIn} au {checkOut}. Check-in : {staySettings.checkIn}. Check-out : {staySettings.checkOut}.</Text><Text style={emailText}>Montant réglé : {(totalAmount/100).toLocaleString("fr-FR",{style:"currency",currency:"EUR"})}.</Text></EmailShell>}
