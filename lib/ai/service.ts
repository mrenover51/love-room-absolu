import type {AbsoluAssistantProvider,AssistantRequest} from './provider';import {DisabledAssistantProvider} from './provider';export class AbsoluAssistant{constructor(private provider:AbsoluAssistantProvider=new DisabledAssistantProvider()){}isAvailable(){return this.provider.isConfigured()}async ask(request:AssistantRequest){if(!this.isAvailable())throw new Error('AI_PROVIDER_NOT_CONFIGURED');return this.provider.complete(request)}}
