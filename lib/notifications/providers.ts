export type NotificationMessage={kind:'reservation'|'payment'|'cancellation'|'sync';recipient:string;title:string;body:string};export interface NotificationProvider{readonly channel:'email'|'push'|'sms';isConfigured():boolean;send(message:NotificationMessage):Promise<{id:string}>}export interface PushSubscriptionRepository{subscribe(userId:string,subscription:unknown):Promise<void>;unsubscribe(userId:string):Promise<void>}
