import "server-only";
import { createAdminClient } from "../admin";
import { BOOKING_CONFIG } from "@/lib/booking/constants";
export class SupabasePricingRepository {async getConfig(){const db=createAdminClient();const[{data:prices,error:pe},{data:options,error:oe},{data:seasons,error:se},{data:settings,error:ste}]=await Promise.all([db.from("pricing").select("weekday,price"),db.from("options").select("option_key,name,description,price,active,order").eq("active",true).order("order"),db.from("seasonal_prices").select("start_date,end_date,price").eq("active",true).order("start_date"),db.from("settings").select("key,value").in("key",["minimum_nights","maximum_nights"])]);if(pe||oe||se||ste)throw new Error("PRICING_READ_FAILED");const rule=(key:string,fallback:number)=>{const value=settings?.find(item=>item.key===key)?.value;return typeof value==="object"&&value!==null&&"value" in value&&typeof value.value==="number"?value.value:fallback};return{weekdayAmounts:Object.fromEntries((prices??[]).map((row)=>[row.weekday,row.price])) as Record<number,number>,seasonalPrices:(seasons??[]).map(row=>({startDate:row.start_date,endDate:row.end_date,amount:row.price})),extras:(options??[]).map((row)=>({key:row.option_key,label:row.name,description:row.description,amount:row.price,enabled:row.active})),currency:BOOKING_CONFIG.currency,minimumNights:rule("minimum_nights",BOOKING_CONFIG.minimumNights),maximumNights:rule("maximum_nights",BOOKING_CONFIG.maximumNights)}}}
