import "server-only";import {createAdminClient} from "../admin";import {BOOKING_CONFIG} from "@/lib/booking/constants";import type {PublicPricingConfig} from "@/lib/booking/types";
export class SupabasePricingRepository{async getConfig():Promise<PublicPricingConfig>{const db=createAdminClient();const[{data:prices,error:pe},{data:options,error:oe},{data:seasons,error:se},{data:promotions,error:pre},{data:settings,error:ste}]=await Promise.all([db.from("pricing").select("weekday,price"),db.from("options").select("option_key,name,description,price,active,order").eq("active",true).order("order"),db.from("seasonal_prices").select("start_date,end_date,price").eq("active",true).order("start_date"),db.from("promotions").select("start_date,end_date,discount_percent").eq("active",true),db.from("settings").select("key,value").in("key",["minimum_nights","maximum_nights","revenue_rules"])]);if(pe||oe||se||pre||ste)throw new Error("PRICING_READ_FAILED");const value=(key:string)=>settings?.find(item=>item.key===key)?.value,rule=(key:string,fallback:number)=>{const item=value(key);return typeof item==="object"&&item!==null&&"value" in item&&typeof item.value==="number"?item.value:fallback},rawRules=value("revenue_rules"),revenueRules=typeof rawRules==="object"&&rawRules!==null&&"lastMinuteDays" in rawRules?rawRules as PublicPricingConfig["revenueRules"]:undefined;return{weekdayAmounts:Object.fromEntries((prices??[]).map(row=>[row.weekday,row.price])) as Record<number,number>,seasonalPrices:(seasons??[]).map(row=>({startDate:row.start_date,endDate:row.end_date,amount:row.price})),promotions:(promotions??[]).map(row=>({startDate:row.start_date,endDate:row.end_date,discountPercent:row.discount_percent})),revenueRules,extras:(options??[]).map(row=>({key:row.option_key,label:row.name,description:row.description,amount:row.price,enabled:row.active})),currency:BOOKING_CONFIG.currency,minimumNights:rule("minimum_nights",BOOKING_CONFIG.minimumNights),maximumNights:rule("maximum_nights",BOOKING_CONFIG.maximumNights)}}}
