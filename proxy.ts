import type {NextRequest} from 'next/server';import {NextResponse} from 'next/server';import {createClient} from '@supabase/supabase-js';import {updateSession} from '@/lib/supabase/proxy';
export async function proxy(request:NextRequest){if(request.nextUrl.pathname.startsWith('/admin'))return updateSession(request);if(request.nextUrl.pathname==='/maintenance')return NextResponse.next();const url=process.env.NEXT_PUBLIC_SUPABASE_URL??process.env.SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY??process.env.SUPABASE_SERVICE_KEY;if(url&&key){try{const{data}=await createClient(url,key,{auth:{persistSession:false}}).from('settings').select('value').eq('key','maintenance').maybeSingle();const value=data?.value as {enabled?:boolean}|null;if(value?.enabled)return NextResponse.rewrite(new URL('/maintenance',request.url))}catch{console.error('maintenance_state_unavailable')}}return NextResponse.next()}
export const config={matcher:['/admin/:path*','/((?!api|_next/static|_next/image|favicon.ico|robots.txt|sitemap.xml|maintenance).*)']};
