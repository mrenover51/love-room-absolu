import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import { canRevealKeybox, parisParts, portalExpired } from "../lib/guest-portal/eligibility.ts";
const read=file=>readFileSync(new URL(`../${file}`,import.meta.url),"utf8");
const repository=read("lib/guest-portal/repository.ts"),migration=read("supabase/migrations/202609110043_guest_portal_communications.sql"),communications=read("lib/guest-portal/communications.ts"),webhook=read("app/api/stripe/webhook/route.ts"),ical=read("lib/calendar/sync.ts"),outbound=read("lib/calendar/outbound-export.ts"),portal=read("app/mon-sejour/[token]/page.tsx");
const base={checkIn:"2026-09-25",checkOut:"2026-09-27",status:"confirmed",paymentStatus:"paid",source:"direct",revealTime:"14:00"};
test("portail: token fort, unique et lookup exclusivement par token",()=>{assert.match(migration,/encode\(gen_random_bytes\(32\), 'hex'\)/);assert.match(migration,/unique index[\s\S]*guest_portal_token/i);assert.match(repository,/TOKEN=\/\^\[0-9a-f\]\{64\}\$\//);assert.match(repository,/\.eq\("guest_portal_token",token\)/);assert.doesNotMatch(portal,/stripe_payment_intent|stripe_checkout_session/)});
test("portail: avant 14h le code est refusé, après 14h il est permis en heure de Paris",()=>{assert.equal(canRevealKeybox({...base,now:new Date("2026-09-25T11:59:00Z")}),false);assert.equal(canRevealKeybox({...base,now:new Date("2026-09-25T12:00:00Z")}),true);assert.deepEqual(parisParts(new Date("2026-09-25T12:00:00Z")),{date:"2026-09-25",time:"14:00"})});
test("portail: annulation, remboursement, impayé direct et fin de séjour masquent le code",()=>{assert.equal(canRevealKeybox({...base,status:"cancelled",now:new Date("2026-09-25T13:00:00Z")}),false);assert.equal(canRevealKeybox({...base,status:"refunded",now:new Date("2026-09-25T13:00:00Z")}),false);assert.equal(canRevealKeybox({...base,paymentStatus:"unpaid",now:new Date("2026-09-25T13:00:00Z")}),false);assert.equal(canRevealKeybox({...base,now:new Date("2026-09-27T13:00:00Z")}),false);assert.equal(portalExpired("2026-09-27",7,new Date("2026-10-05T10:00:00Z")),true)});
test("le code n'est même pas sélectionné avant la décision serveur",()=>{const first=repository.indexOf('.select("reference'),decision=repository.indexOf('if(!expired&&canRevealKeybox'),sensitive=repository.indexOf('.select("keybox_code")');assert.ok(first<decision&&decision<sensitive);assert.doesNotMatch(repository.slice(first,decision),/keybox_code/)});
test("communications: unicité et claim empêchent les doubles envois",()=>{assert.match(migration,/unique \(reservation_id, type\)/i);assert.match(communications,/\.is\('sent_at',null\)/);assert.match(communications,/status:'processing'/);assert.match(communications,/ignoreDuplicates:true/);for(const type of ['confirmation','pre_arrival','access_ready','checkout_reminder','post_stay_review'])assert.match(migration,new RegExp(`'${type}'`))});
test("Stripe confirme puis met en file sans envoi Resend synchrone",()=>{assert.match(webhook,/await queueConfirmation\(id\)/);assert.doesNotMatch(webhook,/await sendConfirmation/);assert.match(webhook,/payments\.confirm/)});
test("Booking et Airbnb: aucune adresse iCal technique n'est envoyée et export inchangé",()=>{assert.match(ical,/@invalid\.local/);assert.match(communications,/endsWith\("@invalid\.local"\)/);assert.doesNotMatch(outbound,/guest_portal_token/);assert.doesNotMatch(ical,/guest_portal_token/)});
test("SMS reste une abstraction sans fournisseur activé",()=>{const providers=read("lib/notifications/providers.ts");assert.match(providers,/channel:'email'\|'push'\|'sms'/);assert.doesNotMatch(communications,/Twilio|sendSms|smsEnabled\s*===\s*true/i)});
